using Gtk 4.0;
using Adw 1;

template $HistoryPage : Adw.Bin {
  Adw.ToolbarView {
    [top]
    Adw.HeaderBar {}
  
    content: ScrolledWindow {
      hexpand: true;
      hscrollbar-policy: never;

      $InlineSongListView list_view {}
    };
  }
}
